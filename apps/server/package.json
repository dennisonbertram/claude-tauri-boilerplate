{
  "name": "@claude-tauri/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "lsof -ti :3131 | xargs kill 2>/dev/null; bun --watch src/index.ts",
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile dist/server"
  },
  "dependencies": {
    "@ai-sdk/provider-utils": "^2",
    "@anthropic-ai/claude-agent-sdk": "^0.2.76",
    "@claude-tauri/shared": "workspace:*",
    "ai": "^6.0.116",
    "googleapis": "^171.4.0",
    "hono": "^4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
