{
  "name": "@claude-tauri/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile dist/server"
  },
  "dependencies": {
    "hono": "^4",
    "@anthropic-ai/claude-agent-sdk": "^0.2.76",
    "@ai-sdk/provider-utils": "^2",
    "@claude-tauri/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
