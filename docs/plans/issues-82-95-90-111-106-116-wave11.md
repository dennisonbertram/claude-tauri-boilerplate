# Wave 11 Plan (Issues #82, #90, #95, #106, #111, #116)

## #116 CPU and memory usage display

- [x] Inspect current status/settings UI for diagnostics support.
- [x] Add tests for status bar resource segment and settings toggle behavior.
- [x] Wire diagnostics endpoint to status bar with safe polling.
- [x] Add regression checks for no-op when diagnostics are disabled.
- [ ] Add manual browser-control verification: open Settings → Status, toggle **Show Resource Usage**, confirm the status bar updates with CPU and memory values, then reopen Settings to confirm persistence.

Notes:

- The code and targeted automated tests are complete in `issue-116-wave13`.
- The app already had the backend diagnostics route and process idle-timeout cleanup; no extra backend cleanup was needed for this pass.

## #106 Environment variable management in settings

- [x] Add Settings UI fields for creating, editing, and removing runtime environment variables.
- [x] Wire runtime env state through `ChatRequest` to the chat runtime environment.
- [x] Add tests for UI persistence and runtime env propagation, including env-clearing behavior.
- [ ] Add manual browser-control verification: open Settings → General, add/edit/remove a runtime env var, close and reopen Settings to verify values persist, then send a chat request and confirm no `.env` values were auto-loaded.

## #111 Workspace creation from existing branch + GitHub issue payload support

### Scope

- Reuse the current workspace creation backend flow while adding support for:
  - `sourceBranch` as a workspace creation input.
  - `githubIssue` payload mapping to workspace issue metadata.
- Extend frontend workspace creation input plumbing so users can optionally provide a source branch from the create-workspace dialog.
- Preserve the existing customizable workspace branch-prefix flow already on `main`.

### Completed

- [x] Added `CreateWorkspaceRequest.sourceBranch` and `CreateWorkspaceRequest.githubIssue`.
- [x] Extended workspace creation route schema to accept `sourceBranch`, `branchPrefix`, and `githubIssue`.
- [x] Reused existing create-workspace orchestration flow:
  - map `sourceBranch` (or `baseBranch`) into effective workspace base.
  - preserve configurable branch prefix for the created branch name.
  - validate missing source branch early.
- [x] Added backend route tests for branch-prefix, source-branch, and GitHub issue creation paths.
- [x] Extended `CreateWorkspaceDialog` UI and hook/API signatures to pass through optional source branch.
- [x] Added dialog tests for source-branch callback payload.

### Remaining

- [ ] End-to-end UX for creating a workspace directly from a GitHub issue selection flow.
- [ ] End-to-end UX for picking an existing repository branch from UI instead of free-form text.
- [ ] Manual browser-control verification: create a workspace from the Workspaces dialog using both a custom branch prefix and a valid source branch, then confirm invalid source-branch input surfaces an error and the Linear issue creation flow still works.
