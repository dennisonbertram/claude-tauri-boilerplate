# Wave 7 (Open Issues: #78, #83, #84, #91, #110, #117)

## Scope

1. #78 — Linear integration for issue tracking (`partially-done`, verify and finish remaining work or add regression tests)
2. #83 — Checkpoints: revert chat to any previous turn
3. #84 — Plan mode with interactive questions and feedback
4. #91 — Slash commands with autocomplete
5. #110 — General agent instructions (system prompt customization)
6. #117 — Model selector with fast switching and default model control

## Execution plan

- Read current live issue details and branch/state from GitHub before implementing (done).
- Use per-issue isolated worktrees (up to 6 at once):
  - `codex/issue-78-linear-integration` (existing worktree)
  - `codex/issue-110`
  - `codex/issue-83`
  - `codex/issue-84`
  - `codex/issue-91`
  - `codex/issue-117`
- Add or extend failing tests first for each touched behavior.
- Implement minimal fixes and keep scope to issue acceptance criteria.
- Re-run targeted tests for each worktree.
- Add a short manual browser verification note for each issue in `docs/plans/` so another agent can execute quickly.
- Merge into integration branch `codex/wave7-merge`, then run post-merge focused validation.

## Per-issue completion checklist

### #78 Linear integration (partially done)
- [ ] Confirm whether existing implementation on current integration branch/PR already covers remaining acceptance criteria.
- [ ] If already implemented: add missing regression tests and mark as `partially-done` on GitHub with handoff details.
- [ ] If gaps remain: implement remaining work with tests first, then document completion status.
- [ ] Add manual browser-control verification note.

### #83 Checkpoints: revert chat to any previous turn
- [ ] Add regression test for checkpoint creation and revert behavior.
- [ ] Implement revert semantics for chat history + workspace files.
- [ ] Add guarded UI flow + confirmation.
- [ ] Add manual browser-control verification note.

### #84 Plan mode with interactive questions and feedback
- [ ] Add tests for plan mode gating and state transitions.
- [ ] Implement plan mode toggle + user approval UX.
- [ ] Add interactive follow-up question handling path.
- [ ] Add manual browser-control verification note.

### #91 Slash commands with autocomplete
- [ ] Add tests for `/` matcher, fuzzy command suggestions, and invalid-command errors.
- [ ] Implement autocomplete behavior in composer and ensure plugin command compatibility.
- [ ] Add manual browser-control verification note.

### #110 General agent instructions
- [ ] Add tests for startup instruction injection and per-repo scoping.
- [ ] Implement instruction persistence and runtime injection.
- [ ] Ensure permission mode updates reach agent in-session.
- [ ] Add manual browser-control verification note.

### #117 Model selector with fast switching and default model control
- [ ] Add tests for model default persistence, key switching, and fork carryover.
- [ ] Implement fast model switching UX and settings defaults.
- [ ] Add fast mode and PR cheap model controls.
- [ ] Add manual browser-control verification note.

## Merge and validation plan

1. Merge issue branches into `codex/wave7-merge` in one wave.
2. Run targeted test suites for modified domains.
3. Run browser-flow smoke checks for each issue using the existing `1420` instance when available.
4. Mark GitHub issues:
   - `partially-done` for already-fixed issues with regression coverage notes.
   - `resolved` comments for completed fixes with commit and manual test guidance.
5. Merge `codex/wave7-merge` to `main` only if the branch and `main` are conflict-free and clean.
