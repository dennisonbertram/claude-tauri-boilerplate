# Wave 5: Bug + partially-done issue completion (#73, #75, #76, #77, #79, #80)

## Summary

- Start from `codex/wave4-merge` as the integration baseline.
- Rebase/reuse the existing partial implementations from `codex/issue-75`, `codex/issue-76`, `codex/issue-77`, `codex/issue-79`, and `codex/issue-80` where needed.
- Finish remaining acceptance gaps, add regression tests first where missing, and update issue writeups with manual browser-control checks.

## Status

- **Result**: Completed through verification on `codex/wave5-merge`.
- **Verification date**: 2026-03-17.

## Scope and acceptance

- `#73` New Chat behavior after selecting empty sidebar sessions
  - [x] Confirmed existing fix is present (`fix(#73): use claudeSessionId as proxy for session hasMessages in onSelectSession`) and regression test in `apps/desktop/src/components/__tests__/NewChatBehavior.test.tsx` is green.
  - [x] Manual note added in handoff: click any empty session then "New Chat" does not leave stale active state.
- `#75` Forward failing CI checks to Claude for auto-fix
  - [x] Added parser and `Fix Errors` wiring with regression coverage in `useStreamEvents.ts` and related tests.
  - [x] Manual note added in `docs/implementation/issue-75-wave-1-subagent.md`: trigger tool failure + confirm `Fix Errors` behavior.
- `#76` Provider support (Anthropic/Bedrock/Vertex/custom)
  - [x] Added provider selection, persistence, and env propagation with server + desktop regression coverage.
  - [x] Manual note added in `docs/implementation/issue-76-wave-1-subagent.md`: swap provider + verify request env paths.
- `#77` Command palette (Cmd+K)
  - [x] Added command registry, palette UI, and navigation with regression coverage.
  - [x] Manual note added in `docs/implementation/issue-77-wave-1-subagent.md`: open Cmd+K and execute navigation actions.
- `#79` File attachments + @ mentions
  - [x] Added picker, drag/drop, @mentions, and message preview UX with regression tests in `ImageFeatures.test.tsx`.
  - [x] Manual note added in `docs/implementation/issue-79-wave-1-subagent.md`: attach files, drag/drop, and @-mention selection.
- `#80` Integrated diff viewer
  - [x] Added historical/diff/range coverage and inline comment rendering path with regression tests in workspace diff suites.
  - [x] Manual note added in `docs/implementation/issue-80-wave-1-subagent.md`: open Diff tab and exercise range/review flow.

## Execution

1. Create per-issue worktrees/branches:
   - `codex/wave5-issue-73` from `codex/wave4-merge`
   - `codex/wave5-issue-75` from `codex/wave4-merge`
   - `codex/wave5-issue-76` from `codex/wave4-merge`
   - `codex/wave5-issue-77` from `codex/wave4-merge`
   - `codex/wave5-issue-79` from `codex/wave4-merge`
   - `codex/wave5-issue-80` from `codex/wave4-merge`
2. Each issue branch runs tests first, then implementation for missing gaps.
3. Merge issue branches into `codex/wave5-merge`.
4. Run focused server/frontend validations on merge branch.
5. Run a cleanup/review pass for red tests or conflicts.
6. Add `partially-done` labels + comment with commit/branch + manual test notes.
7. Merge only to `main` if clean and conflict-free.
