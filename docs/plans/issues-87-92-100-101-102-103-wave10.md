# Wave 10 plan: Issues #87, #92, #100, #101, #102, #103

## Goal
Implement six frontend feature issues in parallel from current integration branch `codex/wave10-merge` and integrate them into a single merge branch before landing.

## Selected issue order (simplest first)
1. #103 Notifications, sound effects, and unread indicators
2. #87 Claude thinking display and thinking level controls
3. #92 Response metadata and AI message actions
4. #100 Terminal improvements: search, reading, and full-height expansion
5. #102 LaTeX rendering and Mermaid diagram support
6. #101 Git status sidebar, branch editing, and custom branch prefix

## Per-issue branches
- #103: `codex/issue-103-wave10` (`.claude/worktrees/issue-103-wave10`)
- #87: `codex/issue-87-wave10` (`.claude/worktrees/issue-87-wave10`)
- #92: `codex/issue-92-wave10` (`.claude/worktrees/issue-92-wave10`)
- #100: `codex/issue-100-wave10` (`.claude/worktrees/issue-100-wave10`)
- #102: `codex/issue-102-wave10` (`.claude/worktrees/issue-102-wave10`)
- #101: `codex/issue-101-wave10` (`.claude/worktrees/issue-101-wave10`)

## Process checklist
- [x] Confirm issues are currently not marked `in-progress` before assignment.
- [x] Mark all selected issues as `in-progress`.
- [x] Create per-issue worktrees/branches from `codex/wave10-merge`.
- [ ] Write tests first for each issue in its branch.
- [ ] Implement each issue.
- [ ] Re-run target tests per branch and fix regressions.
- [ ] Add/update manual browser-control verification notes.
- [ ] Merge all issue branches into `codex/wave10-merge`.
- [ ] Run targeted validation on integration branch.
- [ ] Cleanup/review pass for integration conflicts.
- [ ] Mark issues `partially-done` with comment pointing to integration branch and verification notes.
- [ ] If clean, land `codex/wave10-merge` to `main`.

## Per-issue manual browser-control verification notes
- #103
  - Open `http://localhost:1420`, trigger a completed assistant response, verify notification banner/OS notification appears and unread indicator updates only at completion.
- #87
  - Open one chat, send a prompt that generates assistant reasoning, toggle thinking (`Option+T`) and use `Cmd+Shift+.` to expand/collapse.
- #92
  - Open a rich response, inspect message footer for duration/token metadata and verify hover/details and action buttons.
  - Hover metadata and attempt copy-as-markdown plus retry path for failed assistant responses.
- #100
  - Open terminal, run commands, use `Cmd+K` to clear, `Cmd+F` opens search; verify terminal can be set full-height.
- #102
  - Render LaTeX in a chat message and a mermaid diagram; verify pan/zoom and fullscreen controls work.
- #101
  - Open workspace sidebar, confirm git status summary, inline branch rename, and branch prefix setting persistence.

## Notes
- Keep conflict resolution explicit when merging; avoid wholesale conflict acceptance.
- If any issue is already effectively complete on the integration line, document and add regression tests only, then keep status as `partially-done` with note.
