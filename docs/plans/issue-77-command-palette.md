# Issue #77 command palette wave-1 plan

- [ ] Add command list support for session and PR navigation.
- [ ] Keep fuzzy matching in command filtering with relevance ordering.
- [ ] Expose navigation callbacks from root app to ChatPage command context.
- [ ] Ensure Cmd+K opens/toggles the palette (and remains wired through keyboard hooks).
- [ ] Add regression tests for new commands and fuzzy matching.
